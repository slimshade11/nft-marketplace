import { ToastMessage } from '@common/models/toast-message.model';

export const LoadContractError: ToastMessage = {
  severity: 'Error!',
  details: 'Something went wrong during loading contract',
};
