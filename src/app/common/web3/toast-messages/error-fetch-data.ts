import { ToastMessage } from '@common_models/toast-message.model';

export const ErrorFetchData: ToastMessage = {
  severity: 'Error!',
  details: 'Something went wrong during fetching initial web3 data',
};
